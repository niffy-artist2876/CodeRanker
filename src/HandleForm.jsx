/**
 * Deprecated: HandleForm has been removed.
 *
 * The functionality is now provided by the inline `LinkForm` inside `Progress.jsx`.
 * This stub remains to avoid breaking any lingering imports during the transition.
 */

export default function HandleForm() {
  // No-op component
  return null;
}
